# 콜레이더 전수 점검 — 2026-08-25

오늘 터진 버그 네 가지의 **패턴**으로 앱(62파일 20,297줄)과 서버 전체를 훑었습니다.
발견은 전부 조사 결과이고, **아무것도 고치지 않았습니다.** 우선순위는 같이 정하시면 됩니다.

---

## 요약 — 급한 순서

| | 무엇 | 인원·범위 | 복구 |
|---|---|---|---|
| 1 | 백업 복원 실패를 성공으로 착각 → 서버 백업 덮어씀 | **실측 6명** (진행 중) | 불가 |
| 2 | `driver_type` 기본값 `personal` = 미설정 | 전체 | — |
| 3 | 일일마감 재저장 시 사진·가스비 소실 | 소급 제출자 | 불가 |
| 4 | 권한 토글이 복귀 후 갱신 안 됨 | 전체 | — |
| 5 | 수수료 0이 서버값 덮어씀 | 재설치자 | 불가 |

---

## 1. 백업 자멸 — 실측 확인함

**코드** `BackupSync.kt:110-120`

```kotlin
} catch (e: Exception) { Log.e("CallRadar", "복원 실패: ${e.message}") }
onDone?.invoke()        // ← 실패해도 무조건 호출
```

`fetch()`가 네트워크 오류로 null을 반환해도 `onDone`이 실행되고,
`MainActivity.kt:167`이 거기에 `backupRestored = true`를 걸어둡니다.
**복원 실패가 복원 성공으로 기록됩니다.**

그 뒤 `onStop` → `pushPayroll`이 `prefs.contains(k)`인 키만 모아
서버 번들을 통째로 교체합니다(`backup.js:31` `DO UPDATE SET val=EXCLUDED.val`).

**왜 자주 터지나** — `BackupSync`의 타임아웃이 8초인데,
같은 코드베이스 `MainActivity.kt:263`에 이렇게 적혀 있습니다:
> "Render 콜드스타트(최대 30~50s) 대비 최대 2회 시도"

기변 후 첫 실행 = 서버가 자고 있을 확률이 가장 높은 순간인데, 백업 복원만 8초에 포기합니다.

**실측 (365명 중)**

| 판정 | 인원 |
|---|---|
| 얇은 백업(5키 이하) | 178 |
| **실제 덮어쓴 증거** | **6** |
| 운행 5건 미만 신규 (정상) | 161 |
| 보류 | 11 |

처음엔 "150명 유실"로 보였으나, 대부분 설정을 만진 적 없는 신규 유저였습니다.
서버 설정에 값이 있는데 백업엔 없는 경우만 셌더니 6명입니다.

**진행 중인 증거** — 397번(8/25), 163번(8/25 · 사납금 235,000원 · 운행 267건)

---

## 2. `driver_type` — affiliation과 같은 구조, 영향은 더 큼

기본값이 `'personal'`인데 그게 선택지 중 하나입니다. 오늘 고친 `affiliation='none'`과 똑같습니다.

**세 갈래로 터집니다**

1. **정산 과대** — `HomeScreen.kt:567` `effDailySanap = if (isCorporate) dailySanap else 0`
   법인기사가 personal로 잡히면 사납금이 안 빠져 하루 11~14만원 부풀려짐
2. **점검 무력화** — `index.js:3154` 사납금 점검이 `driver_type==='corporate'` 조건.
   personal로 되돌아간 법인기사는 **점검에도 안 잡혀 조용히 틀린 값**을 봄
3. **데이터 파괴** — `MoreScreen.kt:1464` `if (driverType=="personal") { putInt("daily_sanap", 0) }`
   기본값 personal인 채 저장하면 **사납금이 지워짐**

`driver_type_chosen` 플래그가 이미 있지만(`MainActivity.kt:296`) 온보딩 팝업 판정에만 쓰이고
`BackupSync.kt:27` BOOL_KEYS에 없어 재설치하면 사라집니다.

**참고** — `auto_record_touched`(`NaviIntentReceiver.kt:237`)와 `toll_src`(`tolls.js:160`)가
이미 이 문제의 정답 패턴입니다. 새로 설계할 필요 없이 그대로 쓰면 됩니다.

---

## 3. 일일마감 재저장 — 오늘 고친 것과 같은 코드가 그대로

**`index.js:3213-3240`**

```js
ON CONFLICT (user_id, work_date) DO UPDATE SET
  meter_photo_url=$6, gas_photo_url=$7, gas_cost=$4, ...
[.., meter_photo_url||'', gas_photo_url||'', gas_cost||0, ..]
```

앱은 화면 진입 시 **그 날짜의 기존 행을 읽지 않습니다.**
`meterUrl`/`gasUrl`은 `null`로 시작해 새로 찍어야만 채워집니다.

같은 날짜로 두 번 저장하면:
- 전표·가스영수증 **사진 링크 소실** (Cloudinary 파일은 남지만 찾을 수 없음)
- `index.js:3227`이 그날 LPG 지출을 **무조건 DELETE** 후 `gas_cost>0`일 때만 재삽입 → 지출 사라짐

어제·그제 소급 제출을 지원해서 이 상황이 자주 생깁니다.

---

## 4. 권한 토글이 복귀 후 갱신 안 됨 — 세 곳

오늘 고친 설치 점검 카드와 **같은 계열**인데 자리가 다릅니다.

| 파일 | 무엇 | ON_RESUME |
|---|---|---|
| `SimpleHomeScreen.kt:269,274,276` | 자동설정 3종 토글 | 있으나 블록 안에 갇힘 |
| `HomeScreen.kt:1171,1173,1183` | 기록 설정 3종 토글 | 있으나 서버 데이터만 |
| `MoreScreen.kt:629` | 운행 버튼 | **아예 없음** |

증상이 같습니다 — 스위치 켬 → 시스템 설정에서 권한 허용 → 돌아옴 → **스위치가 여전히 꺼짐.**
게다가 `startFloatingButton()`이 권한 없으면 `return`하므로 버튼도 안 뜹니다.
한 번 더 눌러야 동작합니다.

`SetupGuideScreen.kt:82-89`가 올바른 레퍼런스입니다.

---

## 5. 수수료 0이 서버값을 덮어씀

`MoreScreen.kt:1163-1178` — 설정 화면에서 **뭘 고치든 전 필드를 매번 전송**합니다.

```kotlin
put("commission_rate", (feeRateFloat(prefs,"fee_kakao") + ... ).toDouble())
```

`fee_*`는 로컬 전용이고 `SettingsSync`가 **의도적으로 복원하지 않습니다**(합산값이라 되돌릴 수 없음).
재설치 후 로컬 0 → `commission_rate: 0.0` 전송.

오늘 제가 넣은 서버 방어는 `undefined/null/''`만 막고 **숫자 0은 통과**시킵니다.
`daily_payment` 한 필드의 법인 조합 검사만 추가됐습니다.

---

## 그 밖에 (낮음~보통)

- `payment_type` 기본 `'auto'` — 현금 운행이 과세매출로 들어감. 이미 두 번 사고 났고 파서만 고침
- `lpg_price` 기본값이 앱 UI는 1050, 저장 경로는 0 — 그럴듯한 값이라 "입력함"으로 오판
- `work_days` 기본 26 — 만근 25일 회사면 사납금 하루치 과다. **회사 제출 엑셀에 들어감**
- `day_start_set` 플래그가 `BackupSync` BOOL_KEYS에 빠짐 — 시각은 복원되는데 플래그만 사라져 표시와 계산이 어긋남
- `/api/trips`는 `fare=EXCLUDED.fare`인데 `/api/trips/manual`은 `COALESCE` — 정책이 어긋남
- `closeStaleSessions`가 진행 중 근무를 끄면 근무영수증이 안 남음

---

## 판단

**1번만 서버 한 줄로 지금 멈출 수 있습니다.** "기존 번들보다 키가 적으면 거부"를 넣으면
데이터를 지우지 않고 지우는 것만 막습니다. 앱 업데이트를 안 기다립니다.

나머지는 앱 수정이 필요해서 다음 빌드에 묶어야 합니다.

2번(`driver_type`)이 제일 넓지만, 고치려면 마이그레이션 판단이 필요합니다 —
지금 `personal`인 사람 중 누가 진짜 개인이고 누가 미설정인지 이제 와서 못 가릅니다.
`driver_type_chosen`을 백업 키에 넣고 앞으로 쌓이는 것부터 정확하게 하는 게 현실적입니다.
