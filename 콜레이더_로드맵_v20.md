# 콜레이더 다음 작업 로드맵

_최종 정리: 2026-07-30_

> **순서 원칙: v19 먼저 심사 통과 → v20에서 아래 AI 기능 착수.**
> (v19 = 스샷권한 기반 + 버그·UI·기능 업뎃 + 오늘 보안수정. v20 = AI 자동학습·자동기록.)

---

## 0. v19 관련 (지금 진행 중, 먼저 끝낼 것)
- v19 aab는 **versionCode 19 / versionName 1.9 + 오늘 보안수정(IDOR) 포함**해서 빌드 완료됨.
- 순서: **v18 포그라운드서비스(스샷) 권한 선언 승인 → v19를 비공개 테스트(Alpha)에 업로드 → 통과 후 프로덕션.**
  - 스샷 권한 시연영상은 앱 콘텐츠 단위라 **기존 영상 재사용**(새로 안 찍어도 됨).
  - v18 초안은 따로 출시 안 하고 v19로 대체 가능.
- **보안 phase-2 (남은 것)**: 앱 v19가 충분히 퍼지면 → 서버를 **토큰 강제 모드**로 전환(코드 몇 줄)해서 IDOR 완전 차단. 지금 하면 구버전 접속 끊기므로 대기.

---

## 1. v20 목표: AI 자동학습·자동기록 ("관리형 진화")

### 핵심 구조
- **폰 = 인식(쉬운 일), 서버 = 학습(어려운 일).**
- 폰은 규칙으로 인식만 하고, 서버가 규칙을 개선해서 앱에 push. (앱 업데이트 없이 인식률 향상)

### A단계 — 규칙 자동학습 루프 (지금 인프라로 가능, 저위험)
> **인식 → 유저가 고침 → 서버가 교정 수집 → 규칙 개선 → 앱에 push**
- 토대: 이미 있는 `import_rules`(서버→앱 규칙 전달) 확장.
- 추가: `recognition_feedback` 테이블 + `POST /api/feedback` + 서버 집계 로직.
- **정답이 공짜**: 유저가 종료 시 확정한 값 = 라벨 → 시스템이 자기 채점.
- 서버 쪽은 **앱 업데이트 없이 지금도 배포 가능**(하위호환).

### 자동 안전장치 (사람이 매번 승인 안 해도 되게)
- **카나리 배포 + 자동 롤백**: 새 규칙을 일부(5%)에만 → 교정률 내려가면 확대, 오르면 자동 원복.
- **자기 채점**: 유저 확정값과 AI 추측 자동 비교 → 정확도 스스로 측정(라벨링 불필요).
- **채택 임계치**: 같은 교정이 N명 이상 + 검증셋 개선일 때만 규칙 반영(노이즈 방지).
- **가드레일**: 음수·비상식 금액·정확도 하락 변경은 자동 거부.
- **회귀 감시**: push 후 교정률 급등 시 자동 원복(자가치유).
- → 대표님 역할은 "매번 승인"이 아니라 "가끔 대시보드 확인" 수준.

### 적용 대상 기능 (인식형만)
- **O**: 금액 자동입력(카카오·우버), 영수증 인식, 플랫폼 인식, 출발지·목적지 인식, 결제수단 인식, 가져오기(마감·타앱) 파싱.
- **X (배울 게 없음)**: 수동입력, 통계·정산 계산, 레이더·공항(외부 API), 지도.
- 알려진 이슈: **카카오 금액 파싱은 잘 됨 / 우버는 오류 있었음** → A단계로 우버 교정 쌓이면 자동 개선됨.

---

## 2. 자동화 타이밍 학습 (플로팅 스샷 활용)

### 아이디어
- 지금은 유저가 시작/종료를 직접 누름. **누르는 순간에 1프레임 스샷 + 상황을 로그로 저장** → 자동 라벨 데이터.
- 나중에 자동화할 때 "언제 자동으로 시작/종료할지" 타이밍을 데이터가 알려줌.

### 신호(앱별)
- **시작 감지**
  - 카카오/온다: 화면에 출발지·목적지 텍스트 + "콜 수락" 화면 → 스샷 파싱.
  - 우버: 지도라 텍스트 적음 → GPS(픽업 후 이동) 또는 목적지 뜰 때 파싱.
  - 정답 = 유저가 실제 "시작" 누른 시각.
- **종료 감지**: 요금·금액 화면 뜨는 순간 = 종료 임박. 유저가 금액 입력·"운행종료" 누른 시각 = 정답.

### 중요 원칙
- **연속 캡처 금지** (배터리·프라이버시·심사 지옥). **"누를 때 1장"만** — 현재 스샷 기능이 이 방식이라 잘 맞음.
- 우버 GPS는 배터리·정확도 이슈 → 목적지 파싱과 병행.

### 단계
1. (지금~) 유저 시작/종료 누름 + 그 순간 컨텍스트(파싱텍스트·GPS·앱·금액) 로그 수집.
2. 로그 분석 → 트리거 패턴·타이밍 파악.
3. 확신 높은 상황부터 "자동 시작할까요?" 반자동 제안(유저 확인).
4. 충분히 정확해지면 완전 자동.

---

## 3. B단계 — LLM 파싱 (나중, 정확도 도약용)
- 지저분한 텍스트를 LLM이 구조화(금액·플랫폼·시간). 정규식보다 정확.
- **비용**: 방식이 좌우.
  - 방식 A (매 인식마다, 어려운 케이스만): 테스터 12명 ≈ 월 몇 백 원 / 1,000명 ≈ 월 2~5만 원.
  - **방식 B (학습할 때만 LLM으로 규칙 생성) ⭐추천**: 사용자 수 무관, 월 수천 원. 앱 무료 유지.
- 저가 소형 모델로 충분(영수증 파싱은 어려운 작업 아님).
- **문제**: ①개인정보(외부 API로 데이터 나감→동의·데이터안전 신고) ②지연 1~3초 ③비결정성(가드레일 필요) ④의존성/스파이크(캐싱·규칙 fallback).

---

## 4. 공통 주의 (심사·프라이버시)
- 금액은 **금융 자격정보가 아니라 유저 본인 기록(장부)** → 프라이버시 부담 가벼움. 필요한 건 "기록을 인식 개선에 쓴다"는 투명한 동의 정도.
- **진짜 민감한 지점 = 타앱(카카오·우버) 화면/알림을 읽는 부분.** Play가 여기를 봄. (알림 자동캡처는 현재 심사에서 빼둠 = NOTIF_CAPTURE_ENABLED=false, Manifest 주석.)
- 새 데이터수집·AI 기능은 심사 부담↑ → v19 통과 후 v20에서.

---

## 요약 한 줄
**v19(보안+UI+기능) 심사 통과 → v20에서 "누를 때 1프레임 로그 수집 → 서버 A단계 규칙 자동학습(자동 안전장치 포함) → 반자동 → 완전 자동"** 순으로 자동기록 진화. LLM은 나중에 방식 B로 저비용 도입.
