# 콜레이더 급여명세서 · 급여계산기 스펙

_설계 확정: 2026-08-01 (이영진 실제 명세서 2건 기반)_

## 핵심 흐름 (앱 탑재 계산기)
명세서 **사진 촬영 → 전 항목 OCR → 서버 저장(/api/payslip) → 역산 계산 → 명세서 그대로 표시 + 예상급여 산출.**
명세서 쌓일수록 회사별 급여식 정확도↑ (본인 데이터·동의라 수집 합법).

## 검증된 역산 공식 (바텀라인)
**차인지급액 = 발생금액 − 공제계 + 부가세경감액**
- 다연 실데이터로 검증: 3,769,964 − 676,725 + 479,930 = **3,573,169 ✅**
- 라벨 일부 미인식 시 소계값(발생금액·공제계)으로 보정 → 실수령은 정확.

## 회사 급여 구조 = "회사 프로필" (실시간 예상급여의 파라미터)
급여식은 **[회사 × 근무형태(일차/주간/야간)]** 조합으로 갈림. 같은 기사도 회사·근무형태 바뀌면 명세서 구조가 통째로 다름.

| | 다연상운 (일차) | 서원택시 (주간) |
|---|---|---|
| 사납금(일 기준입금) | **110,000** | **140,000** |
| 가스비 | **기사 전액부담**(회사카드 지출→매출 차감) | **회사 부담** |
| 초과금 | **100% 기사** | **100% 기사** |
| 구조 | 총입금 있음(총 7,523,150→발생 3,769,964) | 임금 755,592만 명세, 실수입 기사(도급성) |
| 계산방법 인쇄 | **숨김**(역산 필요) | **인쇄됨**(공식 그대로 OCR 가능) |

### 실시간 예상급여 공식
```
기사 몫 = (총매출 − 사납금×근무일수) − 가스비(기사부담일 때) + 기본급/수당
```
→ 콜레이더는 **총매출(운행기록)·근무일수(근무세션)·가스비(영수증)를 이미 보유** → 회사 프로필만 알면 **명세서 오기 전에 매일 예상급여 계산.** 명세서는 검증·보정용.

### 회사 프로필 스키마
```
회사프로필[회사명 × 근무형태] = {
  사납금_일, 가스비부담: '기사'|'회사', 초과율(기본 1.0), 기본급식, 수당식
}
입력원: ①기사 입력 ②명세서 역산 ③계산방법 인쇄분 OCR
```
시드: 다연(일차)=사납11만·가스기사·초과100 / 서원(주간)=사납14만·가스회사·초과100

## 숨은 급여식 역산 (계산방법 미인쇄 회사)
`발생금액 = 기본급+수당 + (총입금 − 기준금)×성과율`
- 미지수 2개(기준금·성과율) → **명세서 2장↑(총입금 다른 달)** 이면 연립으로 풀림. 또는 같은회사 여러 기사.
- 이게 "회사가 숨긴 계산을 콜레이더가 밝혀줌" = 데이터 네트워크 해자.

## 구현 상태
- **서버 payslip.js** ✅: 가변항목 저장(JSONB) + 항목분류(발생/공제/부가세경감) + reverseCalc(역산) + `/api/payslip`, `/api/payslip/latest`. 회사별 라벨 학습(LABEL_GROUPS). **역산 실데이터 검증 통과.**
- **앱 (TODO 다음 턴)**:
  1. 명세서 촬영 + **전 항목 OCR 파서**(지급내역·공제내역·계산방법 섹션, 회사명·근무형태).
  2. `/api/payslip` 업로드 + 스캔결과 **화면 표시**(명세서 그대로 + 역산 내역).
  3. **회사 프로필** 저장(사납금·가스·초과율) + **실시간 예상급여**(운행기록×프로필) → 홈 표시.
  4. 급여데이터 **서버 백업**(기기변경 대비) — 현재 prefs만이라 기변 시 소실.

## 주의(이 세션 확인됨)
- "업데이트하면 초기화"는 코드버그 아님 — prefs는 정상 업데이트에 보존. (초기화는 테스트 중 pm clear 때문)
- 명세서 사진은 온디바이스 분석 권장, 서버엔 항목 데이터(+동의 시 원문) 저장.
