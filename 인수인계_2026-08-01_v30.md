# 콜레이더 인수인계 (2026-08-01 · versionCode 30 / 2.3)

> 다음 세션이 이 문서만 읽고 바로 이어갈 수 있도록 정리.
> **AI는 세션이 바뀌면 이전 대화를 기억 못 한다. 이 파일이 유일한 기억장치다. 매 세션 끝에 갱신할 것.**
> 직전 인수인계: `인수인계_2026-07-31.md`(v23까지). 이 문서가 그 이후(v24~v30)를 잇는다.
> 검증된 기술 결정 표: `CALLRADAR_기술결정_기록.md` (같이 볼 것).

---

## 0. 제품 개요
- **앱**: 콜레이더(스토어명 "콜레이더:택시의 신"), 패키지 `com.callradar.app`. Android(Kotlin/Compose). 택시 기사 수입기록·정산·레이더.
- **서버**: Node.js + Express + PostgreSQL @ Render. `https://callradar-server.onrender.com` (무료 티어, 콜드스타트 있음).
- **배포**: Google Play 프로덕션 진행. versionCode 27 승인 → 이후 28·29·30 순차. **Play가 메인, 반드시 유지.**

## 1. 주요 좌표/값 (변경 없음, 07-31 참조)
- 로컬: 앱=`C:\CallRadar`(gradle 루트), 서버=`C:\CallRadar\server`.
- GitHub: `https://github.com/sollbe82-droid/callradar-server`(서버만, 비공개). 앱 소스는 로컬만.
- Play Console 앱ID `4973750051603798660`. 카카오 app `1483101`.
- 빌드: `C:\CallRadar`에서 `set JAVA_HOME=C:\Program Files\Android\Android Studio\jbr` 후 `gradlew.bat`. **Desktop Commander로 실행**(샌드박스 VM은 폰/윈도우 빌드 불가, 마운트만).
- **[중요] flavor 분리됨(v29)**: `assemblePlayRelease`/`bundlePlayRelease` = Play용(접근성 없음). `assembleOnestoreRelease` = 접근성 포함 빌드. 출력 경로: Play AAB = `app/build/outputs/bundle/playRelease/app-play-release.aab`(옛 경로 `bundle/release/app-release.aab`엔 빌드 후 복사해 둠).

## 2. 이번 세션(v23→v30) 한 일

### 2-1. 버그 수정 (Play 라이브 반영)
- **엔드페어 Android 14 캡처 깨짐** → MediaProjectionConfig.createConfigForDefaultDisplay()로 전체화면 강제(ScreenCapturePermissionActivity).
- **가스영수증이 수입으로/날짜 미인식** → ImageImportActivity에 지출/수입 모드 + fuelExpenseKeywords + 날짜 정규식(하이픈).
- **레이더 5km 고정·엉뚱 지역** → GPS 기준 15km, 가까운 순 정렬, 거리 표시(server /api/radar/personal + RadarScreen).
- **조성현 "서버 연결 실패"(특정 유저)** → 자가치유(403→토큰 클리어 재시도) + 진단 에러메시지(SSL=시계, UnknownHost=DNS).
- **(v27) 퇴근 버튼 크래시**: 화면캡처 세션 없을 때 mediaProjection 포그라운드 승격이 SecurityException → 3중 방어(세션없으면 서비스 안깨움 + ACTION_STOP 승격전처리 + startForeground try/catch). `ScreenCaptureService.kt`.
- **(v27) 근무세션 시간당 매출 오류**: 오늘 전체매출을 세션시간으로 나눠 수백만원 표시 → 세션매출(`todayFare - work_start_fare`)만 사용. `HomeScreen.kt`.

### 2-2. 플로팅 공유·화면캡처 전면 제거 (v26~v30) — 이유: MediaProjection 팝업
- v26: 롱프레스 공유 제거. v28: captureOneFrame 기본경로의 앱 공유시트 제거. v30: **플로팅의 화면캡처(endfare/platform) 트리거 전부 제거 + 출근 동의 트리거 제거.**
- **현재(v30) 플로팅 = 시작/종료 전용, 화면캡처 안 함.** → "화면 공유 동의창" 원천 차단. 금액은 종료 후 **QuickEntry 팝업에서 입력**(quick_entry_enabled).
- MoreScreen의 "스샷 크롭 조절 UI" 제거(v30, 죽은 공유용).

### 2-3. 접근성(v9) 빌드 준비 — **단, 대표가 원스토어 논의 보류 지시**
- flavor `onestore`에 `NaviIntentReceiver`(v9 접근성) 등록: `app/src/onestore/AndroidManifest.xml` + `res/xml/accessibility_service_config.xml`(택시앱 4종) + 설명 문자열. Play 빌드엔 안 들어감(구조로 분리 보장, merged manifest 검증됨).
- 원스토어 개발자센터 가입·앱 등록됨(Android AID **OA01007971**, 패키지 동일 `com.callradar.app`, Google Play 패키지네임 입력). 아이콘 512·홍보 1024×578·스크린샷 5장(731×1300) 생성(`C:\CallRadar\_store\`). 등록 필수정보/권한고지/키워드 텍스트 준비됨.
- **블로커**: "위치기반서비스사업 신고필증"(방통위 emsit.go.kr 온라인 신고, 개인 소상공인 특례) 필요 — 미제출. 대표가 개인(사업자등록 없음)으로 신고 예정이었으나 **원스토어 진행 자체를 보류**함.

## 3. ★핵심 결론 (반복 금지 — 자세한 표는 CALLRADAR_기술결정_기록.md)
**"자동결제 요금을 종료 즉시 + 100% 정확 + Google Play"는 동시에 불가능하다(플랫폼 벽).**
- 알림(NotificationListener): 직접결제 카드알림만. **자동결제는 폰에 알림 자체가 안 옴** → 불가.
- 미터기(GPS+요금표, 앱에 이미 있음): 추정이라 통행료·콜비 누락 → 정확 기록엔 부적합.
- MediaProjection(스샷+OCR): OCR 오독(홈의 "목표 300,000" 오인) + Android14 세션마다 재동의 팝업 → 폐기.
- 계정연동(Gridwise식): 카카오모빌리티/우버/티머니가 **기사 정산 API를 외부에 안 열어줌**(길찾기·법인지출 API만). 크리덴셜 스크래핑은 취약·약관위반·보안폭탄 → 불가.
- **AccessibilityService(v9)**: 화면 텍스트 그대로 = 오독0·팝업0으로 **유일하게 100% 작동**. 하지만 **Google Play 금지**.
- → 정확한 자동기록 원하면 Play가 아니어야 함. Play 고수하면 자동결제 금액은 QuickEntry 입력이 현실.
- **금지: 구글 심사 우회(클로킹/숨은 접근성/동적로딩) — 계정정지 리스크. 하지 않음.**

## 4. 현재 상태 / TODO
- **v30 빌드 완료**(Play AAB/APK). 크래시·세션매출·팝업제거 반영. 아직 업로드 여부 미확정(대표가 올리는 중).
- 출시노트(v30): 퇴근 크래시 수정 / 시간당매출 계산 수정 / 종료시 불필요한 공유화면 제거 / 안정성.
- **미검증(실기기)**: v30에서 종료 시 QuickEntry 수동입력 흐름, 그리고 팝업이 실제로 안 뜨는지 대표 폰 확인 필요.
- 서버 `/api/trips` fare 변경 등은 07-31 이후 상태 확인 필요(배포됐는지).
- NotificationListener(CallCaptureService)는 직접결제용으로 켤 수 있음(매니페스트 주석 해제 + 옵트인 토글). 대표 판단 대기.
- 원스토어/접근성 라인: **대표 지시 있을 때까지 보류.** (기술적으로는 준비 완료, 신고필증만 남음)

## 5. 대표 커뮤니케이션 원칙 (이 세션에서 학습)
- **정확도 타협 금지**: 추정/근사는 "기록" 제품에 무의미하다고 명확히 함. 100% 아니면 안 씀.
- **같은 결론 반복 금지**: 위 3번 표를 먼저 확인하고, 이미 안 되는 걸 다시 제안하지 말 것.
- **검증 없이 빌드 넘기지 말 것**: 코드 트레이스·문서(정책)·컴파일로 먼저 검증하고 근거를 제시. (MediaProjection 실동작은 대표 폰 1회 테스트로만 최종 확인 가능 — AI는 폰 없음.)
- 원스토어는 대표가 먼저 꺼낼 때까지 언급하지 말 것.

## 6. 파일 위치
- 기술결정 표: `C:\CallRadar\CALLRADAR_기술결정_기록.md`
- 07-31 인수인계: `C:\CallRadar\인수인계_2026-07-31.md`
- v20 로드맵: `C:\CallRadar\콜레이더_인수인계_v20.md`(실은 로드맵)
- 냉정평가/진화안: `C:\CallRadar\콜레이더_냉정평가_진화안_2026-07-31.md`
- 원스토어 자산: `C:\CallRadar\_store\`(아이콘·배너·스크린샷·APK)

---

# ★ 추가 진행 (같은 날 v31 + 명세서 설계) — 새 세션은 여기부터 이어갈 것

## versionCode 31 / 2.3.1 (미배포·보조폰 검증됨)
이 세션에서 v27~v31 추가 작업. **보조폰(R3CW70QB0NY) adb 설치·실행·홈렌더·크래시0 확인, 세션 시간당매출 수정 실기기 검증(9,037원, 예전 수백만원 버그 사라짐).** adb=`C:\AndroidSdk\platform-tools\adb.exe`.

### 앱 수정 (컴파일 통과)
- **퇴근 크래시**·**근무세션 시간당매출**(세션매출=todayFare−work_start_fare) 수정.
- **플로팅 화면캡처·공유 전면 제거**(v30): 플로팅=시작/종료 전용, 팝업 원천차단. 금액은 QuickEntry 수동입력. MoreScreen 고아버튼(화면스샷공유·꾹눌러공유·종료시금액인식) 제거.
- **로컬우선 트립저장**(fix): createTrip→LocalTripDatabase.savePending 먼저→서버 동기화. **독립리뷰로 버그 A/B/C 발견·수정**: A=syncPendingTrips가 /api/trips(fare무시)→**/api/trips/manual+started_at**로 수정 / B=**client_uuid 멱등키**(복합 user_id+uuid, ON CONFLICT, xmax=0 신규판정) + handlingLocalIds 레이스방지 / C=synced행 fare_pending 재전송(syncFareUpdates). 스키마 v3(client_uuid).
- **가스/전기 원터치**: 홈 바로가기 ⛽가스·🔌전기 → ReceiptScanActivity 프리셋(자동카메라) → **정식 /api/expenses 저장**(캘린더 반영). home_blocks 기본에 gas,elec 추가.
- **지출 날짜 인식 수정**: ReceiptOcrService.extractDate 확장(한글 년월일·2자리연도·하이픈·8자리) + yyyy-MM-dd 정규화. 가스/전기도 영수증 날짜 사용.
- **영수증 학습 feeder**: ImageImportActivity 저장 시 Feedback.send(import_expense/income) → 이미 배포된 feedback.js 학습엔진에 연결(엔진은 index.js 2639 설치·배포됨).

### 서버 신규 (문법·로직 검증, **미배포**)
- **`server/tax.js`**: tax_config(2025 세율·경비율·감가한도) + 계산기(종소세 경비율vs장부 최소세액 + 부가세) + `/api/tax/config`·`/api/tax/report`. index.js에 installTax 연결.
- **`server/payslip.js`**: 급여명세서 가변항목 저장(JSONB) + 항목분류 + **역산(차인지급액=발생−공제+부가세경감)** + `/api/payslip`·`/api/payslip/latest`. **역산 이영진 실데이터 검증 통과(3,573,169 match)**. index.js에 installPayslip 연결.
- **index.js**: /api/trips/manual에 client_uuid 멱등 ON CONFLICT + trips_client_uuid_uniq(user_id,client_uuid) 인덱스.
- **미배포**: 위 서버 3건 git push 안 됨. 배포 = `C:\CallRadar\server`에서 git add·commit·push → Render 자동배포.

### Q&A 답변 완료
- 수동기록 통계 반영: **잡힘**(통계에 source 필터 없음).
- "업데이트하면 초기화": **코드버그 아님**(prefs는 정상 업데이트에 보존). 초기화는 테스트 중 pm clear 탓.

## ★ 다음 할 일 (우선순위)
1. **명세서 앱 OCR+계산기 탑재** ← 대표 최우선. 상세설계=`C:\CallRadar\콜레이더_명세서_급여계산기_스펙.md`. 명세서 촬영→전항목 OCR(지급/공제/계산방법+회사명·근무형태)→/api/payslip 업로드→**스캔결과 화면 표시**+역산. **회사프로필**(사납금·가스·초과율) 기반 **실시간 예상급여**(운행기록×프로필). 시드: 다연(일차)=11만·가스기사·초과100 / 서원(주간)=14만·가스회사·초과100.
2. **세무 탭 UI**(app): /api/tax/report 표시(종소세 경비율vs장부·부가세). 서버는 완성.
3. **급여데이터 서버 백업**(현재 prefs만 → 기변 소실).
4. ⑤-b 잡지출 카테고리 + 세금적용/미적용 토글.
5. ① 로그인 추가(카카오 외 — 보조폰 카카오 없어 테스트난). ② 온보딩 팝업화(현재 전체화면).
6. **배포**: 위 완성분 → 서버 git push + 앱 AAB(v31~) 한 번에.

---

# ★ 추가 진행 (2026-08-02) — 명세서 앱 OCR+계산기 1차 (컴파일 검증됨·미배포)

## 한 일 (다음 할 일 #1 중 "전항목 OCR→업로드→역산 표시" 부분 완료)
- **`app/.../PayslipOcr.kt` 신규**: 급여명세서 전 항목 OCR 파서. ML Kit 좌표 행매칭(각 금액→같은 행 왼쪽 최근접 라벨)으로 **모든 {라벨,금액} 추출** + 회사명·근무형태(일차/주간/야간/격일)·연월(YYYY-MM)·차인지급액. 서버 `payslip.js`의 `LABEL_GROUPS`/`classify`/`reverseCalc`를 **Kotlin 1:1 포팅** → 온디바이스 즉시 역산(오프라인 동작). 역산식 = 발생금액−공제계+부가세경감. 산술 검증: 3,769,964−676,725+479,930=3,573,169 ✅.
- **`app/.../PayslipScanActivity.kt` 신규**: 촬영/갤러리→OCR→**명세서 그대로 표시**(지급/공제/부가세경감/미분류 그룹) + 역산 카드(발생·공제·부가세경감·역산 실수령·명세서 인쇄값 대조·일치여부) → **`/api/payslip` 업로드**(items 전 쌍+소계+take_home+raw_text, 서버가 재분류·학습). 결과 Intent(company/year_month/take_home) 반환.
- **AndroidManifest**: `PayslipScanActivity` 등록.
- **MoreScreen**: "정산·설정" 그룹에 **📄 급여명세서 스캔** 진입점 추가(`PayslipScanActivity.start`). 기존 간이 급여공제 OCR(parsePayslip 5칸)은 그대로 둠.
- **컴파일 검증**: `gradlew :app:compilePlayDebugKotlin --rerun-tasks` **BUILD SUCCESSFUL(exit 0)**, 에러 0(경고는 전부 기존 파일 deprecation). JDK=`C:\Program Files\Android\Android Studio\jbr`(JAVA_HOME set 시 경로에 **후행 공백/따옴표중첩 주의**, `set JAVA_HOME=...jbr& gradlew` 처럼 `&` 앞 공백 없이).

## 남은 것 (명세서 기능 다음 턴)
- **회사프로필**(사납금·가스부담·초과율) 저장 + **실시간 예상급여**(운행기록×프로필) 홈 표시 — 아직 안 함. 시드: 다연(일차)=11만·가스기사·초과100 / 서원(주간)=14만·가스회사·초과100.
- **급여데이터 서버 백업**(현재 prefs만). 서버 payslips 테이블엔 이미 저장되나 홈/프로필 복원 연결 필요.
- **실기기 미검증**: 실제 명세서 사진으로 전항목 OCR 인식률·좌표매칭 정확도는 대표 폰 1회 테스트 필요(AI 폰 없음). raw_text는 항상 업로드하므로 서버 라벨학습은 누적됨.
- **배포 안 됨**: 서버 payslip.js/tax.js 3건 git push + 앱 AAB(v32~) 아직. (07-31~08-01 미배포분과 함께 한 번에.)

---

# ★★ 추가 진행 (2026-08-02 밤) — 남은 할 일 전부 구현·빌드·배포 (versionCode 31 / 2.3.1)

> **버전 결정**: v31이 Play에 미업로드였으므로 32로 건너뛰지 않고 **versionCode 31 / 2.3.1** 로 이번 배포판을 만듦(대표 지시 "31로 만들어"). Play는 마지막 '업로드된' 코드(=30)보다 크면 되므로 31 유효.

> 대표 "남은 모든거 다 동의, 밤새 만들어놔" 지시로 다음 할 일 #1~#6 일괄 진행. **컴파일·node검증 통과, AAB 빌드 완료, 서버 배포(git push)까지 완료.** 단 실기기 동작은 대표 폰 테스트 필요.

## 신규/수정 파일
### 앱 (Kotlin, 컴파일 통과)
- **CompanyProfile.kt(신규)**: 회사×근무형태 프로필(사납금·가스부담·초과율·기본급) prefs JSON 저장 + 시드(다연 일차 11만/기사/100, 서원 주간 14만/회사/100) + 예상 기사몫 공식 `(총매출−사납×근무일)×초과율 − 가스(기사부담) + 기본급` + 활성프로필 + `applySanapToSettings`(daily_sanap만 안전 반영).
- **CompanyProfileActivity.kt(신규)**: 프로필 목록/선택/편집/추가, **실측 월매출(/api/profile)·근무일수(/api/stats/daily)·가스비(lpg_daily_cost×일)로 실시간 예상 기사몫** 표시. "기사 설정에 적용" 버튼(홈 예상급여 카드 반영). 홈 계산 로직 무손상.
- **PayslipScanActivity.kt(수정)**: "🏢 이 회사 프로필로 저장" 버튼 추가(회사·근무형태·기본급 upsert+활성 → 프로필화면).
- **TaxReportActivity.kt(신규)**: /api/tax/report 표시 — 종소세(경비율 vs 장부 최소세액·추천·절세액) + 부가세(일반=1/11−매입 / 간이=참고) + 연도·과세유형 토글 + disclaimer.
- **BackupSync.kt(신규)**: 회사프로필 + 급여/정산 prefs 번들을 서버 /api/backup 백업. **복원은 기변 첫 실행에서 로컬에 없는 키만 채움**(최신값 덮어쓰기 방지). MainActivity onCreate=복원, onStop=백업. CompanyProfileActivity 변경 시 즉시 백업.
- **MainActivity.kt(수정)**: 온보딩·기사유형선택을 **전체화면→홈 위 팝업**(OnboardingPopup/DriverTypePopup)으로. 기존 전체화면 함수는 미사용으로 남겨둠(삭제 안 함). BackupSync 복원/백업 훅.
- **RecordsScreen.kt(수정)**: 지출 추가에 **세금적용(경비 인정) 토글** 추가(개인지출 기본 off) → POST에 tax_deductible 전송.
- **MoreScreen.kt(수정)**: 진입점 3개 추가 — 📄급여명세서 스캔 / 🏢회사 프로필 / 🧾세무 리포트.
- **QuickEntryActivity.kt(수정 · 대표 피드백)**: ①금액칸 **자동 포커스 + 엔터(완료)로 즉시 저장** ②**속도**: 예전엔 서버 PUT(타임아웃 30초)을 기다린 뒤 팝업을 닫아 콜드스타트 때 느렸음 → **로컬 우선 저장(updateFare, 유실0) 후 팝업 즉시 닫고 서버 PUT·Feedback은 백그라운드 Thread(타임아웃 8초)** 로. 실패해도 fare_pending으로 다음 동기화가 반영. (폴드7 고사양인데도 느린 원인 = UI가 네트워크를 기다린 것.)
- **AndroidManifest.xml**: PayslipScanActivity·CompanyProfileActivity·TaxReportActivity 등록.
- **build.gradle.kts**: versionCode **31**(유지), versionName **2.3.1**(32에서 되돌림 — v31 미업로드라 재사용).

### 서버 (node 문법·모듈로드 검증 통과, **배포됨**)
- **backup.js(신규)**: user_backup(user_id,bkey,val JSONB) 범용 KV 백업. POST /api/backup, GET /api/backup/:user_id/:key. 값은 앱이 JSON 문자열로 송신, 서버는 불투명 저장.
- **index.js(수정)**: expenses에 `tax_deductible BOOLEAN DEFAULT TRUE` 컬럼(ALTER IF NOT EXISTS) + POST /api/expenses에 tax_deductible 반영(개인지출 기본 false) + installBackup 연결.
- **tax.js(수정)**: aggregate 장부 경비를 `tax_deductible IS NOT FALSE AND expense_type<>'personal'`만 집계(개인·미적용 제외).
- **payslip.js/tax.js**: **이번에 처음 git 커밋·push됨**(이전까지 untracked였음). commit d620918, `git push origin main` 성공 → Render 자동배포.

## 검증
- 앱: `gradlew :app:compilePlayDebugKotlin` **BUILD SUCCESSFUL(exit0)**, 이어서 `bundlePlayRelease` **BUILD SUCCESSFUL(1m14s, exit0)** → 서명 AAB 생성. 경고는 전부 기존 파일 deprecation.
- **AAB(versionCode 31)**: `app/build/outputs/bundle/playRelease/app-play-release.aab` → 복사본 **`C:\CallRadar\_store\callradar-v31-play-release.aab`**(약 25.8MB). **Play Console 업로드는 대표가**(콘솔 접근 필요).
- 서버: `node --check` 4파일 OK + `require` 로드 OK. push 완료. **Render 빌드 성공 시에만 교체되므로 실패해도 라이브 서버는 그대로.**

## 남은 것 / 미검증
- **실기기 테스트(대표 폰)**: ①명세서 사진 전항목 OCR 인식률·좌표매칭 ②회사프로필 예상급여 ③세무 리포트 값 ④온보딩 팝업 흐름 ⑤지출 세금토글. AI는 폰 없음 → 코드·컴파일·API까지만 검증.
- **Play 업로드**: v31 AAB 대표가 콘솔에 올려야 라이브. 출시노트(예): 급여명세서 스캔·예상급여, 회사 프로필, 세무 리포트(추정), 지출 경비 토글, 온보딩 개선, 기변 데이터 백업, **운행요금 엔터 저장·저장 속도 개선**.
- **Render 배포 확인**: push 직후라 콜드빌드 진행 중일 수 있음 — /api/tax/config·/api/backup 200 확인 권장.
- **미착수(우선순위 낮음)**: #5 비카카오 로그인 추가(서버 인증 작업 필요, 리스크 있어 보류). 온보딩 팝업은 완료.
- **잔여 세무 정확도**: tax.js 경비율(0.792 등)은 **예시값** — 국세청 택시 업종코드 고시값으로 교체 필요(코드 주석에 명시).

## 관련 스펙 파일
- 세무: `C:\CallRadar\콜레이더_세무기능_스펙.md` (개인/법인/개인택시탭·경비율·면허감가·부가세)
- 명세서·급여계산기: `C:\CallRadar\콜레이더_명세서_급여계산기_스펙.md`
- 기술결정(되는것/안되는것): `C:\CallRadar\CALLRADAR_기술결정_기록.md` (자동결제 금액=접근성뿐, Play금지)

---

# ★★★ 추가 진행 (2026-08-02 밤~새벽 계속) — versionCode 32 / 2.3.2

> 대표 실사용 피드백 + "안 한 거 다 적용" 지시로 이어감. 버전 32(31은 대표 손에 있어 충돌 방지). 앱 컴파일·보조폰 설치 크래시0, 서버 배포됨.

## v32 앱 (컴파일 통과, AAB 빌드됨 — `_store/callradar-v32-play-release.aab` + `bundle/release/app-release.aab`)
- **홈 🧾지출촬영 버튼**: 일반 영수증도 카메라 OCR→자동분류(식비/세차/주차/정비/통행료/LPG)→정식 `/api/expenses` 저장. 기존 사용자 홈에 1회 자동추가 마이그레이션.
- **알림 버그 수정**: WorkSessionService "근무 거리" 알림이 시작 시 0.0km로 만들고 안 바꿔 멈춰있던 것 → onNewLocation에서 실시간 갱신(0.1km 단위, setOnlyAlertOnce).
- **QuickEntry 칩 잘림 수정**: 플랫폼칩 4개(길빵/예약 잘림) → FlowRow 줄바꿈.
- **자체 아이디/비번 로그인**: LoginScreen에 "또는 아이디로 로그인"(로그인/회원가입) — 카카오 없는 폰 대응. 서버 /api/auth/register·login 사용, 게스트 device 계정 승격.

## v32 서버 (node 검증, **배포됨** git push)
- **radar2.js(신규)**: 공차(역산 갭) 효율분석. `/api/radar/dest-risk`(하차 동네별 다음콜 median 대기), `/api/radar/efficiency`(실차율·순 원/분), `/api/radar/hotzone`(동×시간 hazard=콜÷대기시간). LEAD 윈도우, percentile_cont, 표본 3+ 정직성.
- **authlogin.js(신규)**: 자체 로그인(scrypt 해시, users.login_id/password_hash ALTER).
- **index.js**: KOPIS 공연 **대형만 필터**(대중음악/서커스/복합 장르 또는 대형 공연장; 1000석↓ 근사 제외). radar2·authlogin·backup 연결.

## 아직 안 한 것 (다음 순서)
- ~~radar2 앱 화면 연결~~ ✅ **완료**: RadarScreen 좌측 📊 패널 = 실차율/순원분(efficiency) + 목적지 리스크(dest-risk) + hotzone. 서버 3종 라이브 검증(dest-risk 3.5분/40행, efficiency 실차율 48%).
- **GPS 궤적 기능** — 실차/공차 궤적·지도·빈차/공차/대기 시간·PNG 내보내기. 미착수(제일 큼).
- **회사프로필 홈 완전반영** — 가스·초과율(현재 사납금만).
- **세무 실제 경비율** — 국세청 고시값(현재 예시).
- **KOPIS_API_KEY** — 대표가 Render 환경변수 설정해야 공연 실제 수집. **스포츠는 KOPIS에 없어 별도 소스 필요.**
- **로그인** 서버·앱 다 됐지만 **실기기 로그인 흐름 테스트 필요.**

## ★★ 작업 원칙 (대표 지시 2026-08-02) — 반드시 지킬 것
- **꼼꼼하고 세심하게.** 추측으로 "다 됐다" 선언 금지.
- **"완료"는 실제 컴파일/빌드 통과를 확인한 뒤에만 보고.** 코드만 넣고 됐다고 하지 말 것.
- 빌드 시 JAVA_HOME=`C:\Program Files\Android\Android Studio\jbr` (셸에 없음, 매번 set 필요).

## ★ 서버 위치 (확정)
- **진짜 배포 서버: `C:\CallRadar\server`** → GitHub `github.com/sollbe82-droid/callradar-server` → Render 자동배포(callradar-server.onrender.com). 수정·배포는 반드시 여기서.
- `C:\CallRadarServer`(구폴더)는 **폐기됨(_DEPRECATED_README.md)** — 초기 사본, 배포 무관, 지워도 됨. 여기 보고 헷갈리지 말 것.
- 라이브 확인된 서버 동작: register(중복 409 "이미 사용 중인 아이디예요", 비번<4 400), login(틀리면 401), guest. **device_id로 계정 연결됨** — 같은 기기 register/guest/login = 같은 user_id(테스트 확인).

## ★ v37 (2026-08-02) — 로그인 화면 재설계 + 아이디 로그인 폐기
- 로그인 화면 재디자인: **카카오로 시작하기(메인) + 로그인 없이 둘러보기(게스트)** 두 개만 크게. 자동로그인 체크, 하단 "다른 폰 연결 · 다른 계정" 작은 링크. 기존 6개 버튼 난립 정리.
- **아이디/비밀번호 로그인·회원가입 방식 폐기(대표 결정).** 이유: 카카오 계정(kakao_id)과 아이디 계정(device_id/login_id)이 서버상 별도라 섞으면 데이터 갈림 + 복잡성. → UI에서 제거, 카카오+게스트만.
- 코드: MainActivity.kt LoginScreen 재작성. RegisterScreen()·doLogin()·login state는 **미사용 잔존(경고만, 동작 무해)** — 나중에 지워도 됨. 서버 /api/auth/register·login 엔드포인트는 살아있음(다시 쓰려면 UI만 복구).
- 최종 산출물: **`_store/callradar-v37-play-release.aab` (versionCode 37 / 2.3.7).** 컴파일·번들 BUILD SUCCESSFUL 확인. (v35/36은 중간본, v37만 올리면 됨.)
- 계정 연동 사실(서버 코드 확인): 아이디/게스트/페어링은 device_id로 연동(게스트→가입 시 데이터 승격). 카카오는 kakao_id 단독. 통합(linking)은 미구현 — 필요 시 별도 서버작업.

## 배포/버전 메모
- Play: **최신 = v34 AAB(`_store/callradar-v34-play-release.aab`) 대표가 업로드.** (이전 v31~33 안 올렸으면 v34만 올리면 됨. versionCode는 마지막 업로드분보다 크기만 하면 됨.)
- 서버는 이미 라이브(Render 자동배포). 확인됨: /api/radar/dest-risk·efficiency·hotzone, /api/auth/login(429 잠금 포함), /api/tax/report.

---

# ★ v33→v34 (2026-08-02 새벽) — 전 기능 완성 + 실기기 2차 테스트 + 코드 리뷰 반영

## 완성(v33): GPS 궤적(TrackActivity, 실차/공차 지도 비트맵+PNG공유, LocalTrackDatabase, WorkSessionService 저빈도 기록) / 회사프로필 홈 카드 / radar2 앱 효율패널(RadarScreen 📊) / 자체 로그인 UI / 세무 경비율 0.897.
## 실기기 2차 테스트(보조폰 v2.3.3): 홈·TrackActivity·CompanyProfileActivity 화면 스크린샷 확인, 시드 프로필 로드·계산 정상, **세션 전체 FATAL 0.**
## 코드 리뷰 반영(v34):
- **[fix] WorkSessionService 위치콜백·DB를 HandlerThread(백그라운드)로** → 메인스레드 SQLite/ANR 위험 제거.
- **[fix] radar2 hotzone hazard 시간필터 일치** — 분자(콜)·분모(대기)를 같은 시간대로.
- **[fix] 자체 로그인 무차별대입 잠금** — 아이디별 10분내 5회 실패 시 10분 잠금(429).
- **[개선] FloatingTripService 위치 알림 문구 브랜딩** + setOnlyAlertOnce.
## 남은 리뷰 항목(미적용, 별도):
- 알림 2개 완전 통합(포그라운드 서비스 2개 구조라 신중 필요).
- QuickEntry tip/promo 하드킬 유실 → WorkManager 보장(요금·플랫폼은 이미 로컬안전).
- 명세서 OCR 실기기 인식률(대표 폰 사진 테스트 필요).
- 세무 경비율 그해 고시값 홈택스 확인.
- KOPIS_API_KEY(대표가 Render 설정) + 스포츠 소스.
