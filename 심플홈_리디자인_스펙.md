# 콜레이더 심플 홈(카카오식) 리디자인 — 스펙 + 구현현황 (2026-08-14)

## 결론(확정)
- **옵트인**: 기존 UI=기본(default, 무손상). 심플=설정 토글. `home_mode`=`classic`|`simple`.
- 심플 = **무탭 카카오식(B안)**. classic+B 두 개만(A안 폐기). 3모드 안 함.

## v62에서 구현 완료 (2.6.2, 커밋 c87b83f)
- **신규 파일**(classic 무손상, 회귀 격리):
  - `SimpleHomeScreen.kt` — 헤더(공지·메뉴) + 히어로(오늘매출 큰숫자 + 근무중/시간·거리·시간당 + 출근/일시정지/퇴근) + 기능 4카드(2×2). 근무세션은 classic과 **동일 prefs·서버(/api/work-session, /close) 공유** → 모드 바꿔도 세션 이어짐.
  - `SimpleMenuScreen.kt` — B 3열 아이콘 그리드(기록·레이더·공항·정산·궤적·분석·랭킹·명함·지출) + '전체 메뉴(기존 더보기)' + '기본 홈으로 전환'.
- **MainActivity**: `home_mode` 분기. simple이면 `SimpleMain`(무탭 라우팅, 기존 화면 composable 재사용 + `SimpleWrap` 뒤로가기 헤더). 전환은 `recreate()`.
- **MoreScreen '화면·기능'에 "홈 모드(베타)" 토글** — classic↔simple. (심플 메뉴에도 '기본 홈으로' 있음.)
- 홈 4칸 = prefs `simple_home_cards`(기본 records,radar,airport,settlement).

## 남은 것 (다음 세션)
- **하위 화면 B 리스킨**: 현재 심플 모드는 기록·레이더·공항·정산을 **기존 화면 그대로 + B 뒤로가기 헤더**로 감싸 연결(기능 OK, 스타일은 classic). 화면별로 B(요약+둥근 카드리스트)로 재구성 필요 → 폰 확인하며 순차. '기록' B 목업 확정본 있음.
- **홈 4칸 편집 UI**: 현재 고정 기본값. FeatureRegistry 확장해 기사가 교체(⭐)하게.
- **B 메뉴 커스터마이즈**: 지금은 고정 그리드. 배치/추가/제거.
- **히어로 지표 선택**(매출/순수익/목표), 미근무 시 '출근하기' 큰 버튼(구현됨) 폰 확인.

## 검증 상태
- Kotlin 컴파일 + assembleOnestoreRelease BUILD SUCCESSFUL. **실기기 동작(출근/퇴근/일시정지·모드전환·라우팅)은 대표 폰 확인 필요.**
- 되돌리기: 심플 메뉴 '기본 홈으로' 또는 더보기(전체 메뉴)의 '홈 모드' 토글.
