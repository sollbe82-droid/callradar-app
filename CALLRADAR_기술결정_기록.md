# 콜레이더 기술 결정 기록 (되는 것 / 안 되는 것)

> 목적: 같은 논의를 반복하지 않기 위해 **검증된 사실**만 기록한다.
> 갱신일: 2026-08-01

---

## 핵심 난제: "자동결제 요금을 100% 정확하게 자동기록"

기사에게 요금 화면에 뜬 **최종 금액(팁·통행료·콜비 포함)** 을 자동으로 기록하고 싶다.
결제유형: **직접결제**(카드) / **자동결제**(플랫폼 앱 내 결제) / 현금.

### 금액을 얻는 모든 경로와 검증 결과

| 방법 | 자동결제 100% 정확? | 팝업 | Play 허용 | 판정 |
|---|---|---|---|---|
| **NotificationListener**(알림 읽기) | ❌ 자동결제는 폰에 알림 자체가 안 옴 | 없음 | ✅ | 직접결제만 가능. 자동결제 불가 |
| **미터기(GPS+요금표)** | ❌ 추정. 통행료·콜비·플랫폼요금 누락 | 없음 | ✅ | 기록엔 부적합(추정은 기록의 의미가 없음) |
| **MediaProjection(스샷+OCR)** | ❌ OCR 오독(예: 홈의 "목표 300,000" 오인) | **매번 뜸**(Android 14 세션마다 강제) | ✅(비추) | 팝업 + 오독. 폐기 |
| **계정 연동(Gridwise 방식)** | ⭕ 정확하지만 **하루 1회 지연** | 없음 | ✅ | 한국 택시 플랫폼이 **기사 정산 API를 안 열어줌** → 불가 |
| **AccessibilityService (v9)** | ⭕ **화면 텍스트 그대로 = 100% 정확** | **없음**(설정에서 1회 켜기) | ❌ **Play 금지** | 실제로 작동하는 유일한 방법 |

### 결론 (반복 금지)
- **"종료 즉시 + 100% 정확 + Google Play" 는 동시에 불가능하다.** (플랫폼 벽)
- 100% 정확한 자동기록을 하는 유일한 실제 기술 = **AccessibilityService(v9 = NaviIntentReceiver)**. 화면을 픽셀이 아니라 **텍스트 노드**로 읽어서 오독 0, 팝업 0.
- 그 접근성을 **Google Play가 정책으로 금지**한다. → 정확한 자동기록을 원하면 Play가 아니어야 한다.
- 계정연동은 매력적이나 **카카오모빌리티/우버/티머니가 기사 정산 API를 외부에 열지 않음**(길찾기·법인지출 API만 존재). 크리덴셜 스크래핑은 취약·약관위반·보안폭탄이라 제품 토대로 불가.

---

## v9(접근성) 배포 관련 검증

- **문제 1 — 매 업데이트마다 수동 배포**: 생 APK 사이드로드는 자동 업데이트가 없다. → **대체 스토어(자동 업데이트 지원)** 를 쓰면 해결. 생 APK는 최후수단.
- **문제 2 — 기술 유출(APK 디컴파일)**: 안드로이드 앱은 Play든 아니든 디컴파일 가능. → **핵심 요금추출 규칙을 서버로 이전**하면(클라이언트는 raw 텍스트만 전송, 서버가 금액 반환) 디컴파일해도 규칙이 안 보이고, **규칙 개선 시 앱 재배포 불필요**(서버만 갱신). 두 문제를 동시에 완화.
- R8/ProGuard 난독화 병행.

---

## Google Play 정책 사실 (검증됨)

- AccessibilityService 비장애 용도 = 선언 + 고지 + 동의 필요하고, 그래도 "타앱 화면 읽기"는 심사 리스크 높음. `isAccessibilityTool=true` 허위선언 = 앱 삭제 + 계정 정지.
- Android 14+: MediaProjection은 `createVirtualDisplay`를 세션당 1회만 허용 → 재캡처 시 매번 재동의(팝업). `setSurface`로 우회 시도했으나 근본적으로 팝업 1회는 불가피.
- Android 개발자 신원 인증(2027~ 글로벌): Play·사이드로드·타스토어 전부 신원 인증 필요. 단 이건 "신원 확인"이지 "앱 심사"가 아님 → 접근성 배포엔 지장 없음. 이미 Play 개발자면 대부분 충족.

---

## 확정된 기타 결정

- 직접결제 금액 = 택시투데이/카드 결제 알림에서 확보(이미 됨). NotificationListener로 자동 반영 가능.
- 플로팅 버튼: 시작/종료 전용. 공유 기능 전부 제거(v26~v30). 화면캡처(endfare/platform) 제거(v30) → 팝업 원천 차단.
- 미터기 엔진(calcMeterBreakdown, GPS+요금표+심야/시외 할증)은 앱에 이미 존재(MeterActivity). 단 "추정"이라 정확 기록용은 아님.
- 서버: Node/Express/PostgreSQL on Render(무료, 콜드스타트 이슈).

---

## 다음에 이 문서를 볼 때
- "자동결제 금액 자동으로 못 읽나?" → 위 표 참조. **접근성 외 방법 없음.** 다시 알림/미터기/계정연동 제안하지 말 것.
- 새 방법이 나오면 이 표에 한 줄 추가하고 검증 결과를 남길 것.
