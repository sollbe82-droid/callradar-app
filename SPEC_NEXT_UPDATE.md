# 콜레이더 — 다음 업데이트 스펙 (v48 이후 발생분)

> 작성: 2026-08-09. **오늘 실주행 테스트 + 미터기 영수증 대조**로 나온 것들.
> 현재 배포/설치: 앱 v48(2.4.8, 본폰), 서버 배포완료(지오코딩+캐시/카드매칭/영어POI/레이더토글/대시보드/디버그로그잠금).
> 다음 빌드 = v49. 아래를 다 넣는다.

---

## 1. ★ 매출 영수증 OCR 자동정산 (최우선 · 오늘 손으로 검증한 로직)

**목적:** 하루 끝에 미터기 "당일상세거래내역" 영수증 한 장을 찍으면, 앱의 자동기록 트립과 자동 대조해서 **누락 금액 채움 / 수정결제 교정 / 유령트립 정리**를 해준다. 직접결제·티머니 금액이 화면에 안 떠서 0원인 것들을 하루 한 장으로 다 잡는 안전망.

**영수증 포맷 (파싱 대상):**
```
당일상세거래내역
집계시간: 2026/08/09 07:53 ~ 21:07
총합계 : 302,100원
교통카드 : 9,800원        (거래시간 · 카드번호 · 금액)
신용카드 : 90,700원       (거래시간 · 카드번호 · 금액) × N
카카오택시 : 181,800원    (거래시간 · 콜ID · 금액) × N   ← 콜ID 007288053770 형태
티머니 londa : 19,800원   (거래시간 · 콜ID · 금액) × N   ← 콜ID 000107268394, 취소는 -금액
수고 하셨습니다.
```
- 긴 감열지 + 섹션 구조. **기존 영수증 OCR이 인식 못 한 이유 = 이 포맷 전용 파서가 없어서.**
- 티머니는 **취소(-금액)** 가 섞임 → 콜ID별로 net 계산(같은 콜ID의 +/- 합산).
- 현금은 미터기 영수증에 안 찍힘 → 앱의 현금(길빵) 트립은 영수증에 없어도 정상, 삭제 금지.

**매칭 로직 (오늘 실제로 한 것):**
1. 영수증 각 거래를 파싱 → (시각, 플랫폼, 콜ID, 금액).
2. 앱 트립과 매칭:
   - **1순위: 콜ID** (카카오 007288…, 티머니 000107…). 정확·유일. → 아래 3번(콜ID 저장) 선행.
   - 콜ID 없으면(구버전 트립): **플랫폼 + 시각(±20분) + 금액 근접**으로 매칭. (영수증 시각=결제/하차, 앱 started_at=콜수락 → 앱이 몇 분 빠름)
3. 자동 보정:
   - **앱 0원 / 없음 → 영수증 금액으로 채움** (오늘 #4112·#4120 티머니)
   - **앱 금액 ≠ 영수증 → 영수증 우선 교정** (오늘 #4116 4,800→7,800, 미터기 수정결제)
   - **출발=도착·0원·미완료 = 유령트립 → 삭제 후보** (오늘 #4134)
   - 티머니 취소건(net 0) → 앱에 0원/취소로.
4. **요약 화면:** "영수증 302,100 vs 앱 290,800 · 티머니 3건 채움 · 1건 교정 · 유령 1건 정리 → 일치 ✅" + 각 항목 미리보기, 유저가 확인 후 적용.

**서버:** 트립 fare 일괄 PUT(`/api/trips/:id`)로 반영. 멱등키/중복반영 방지.

---

## 2. ★ 미터기 수정결제 누락 수정 (#4116 케이스)

**현상:** 카카오 "손님이 직접결제 하셨나요? 미터기 요금 4,800" 화면에서 앱이 4,800으로 마감 → 기사가 그 뒤 "입력하신 요금이 맞습니까? 7,800"으로 **수정 결제** → 앱은 첫 4,800만 잡고 최종 7,800 놓침. (오늘 3,000원 오차의 원인)

**수정 방향:** **최종 확인 금액을 우선.**
- "손님이 직접결제 하셨나요? 미터기 X"는 **캐시만**(lastDetectedFare) 하고 즉시 마감하지 않음.
- **"입력하신 요금이 맞습니까? Y" / "자동결제 완료 Y"** 가 최종 → 이걸로 마감(=Y).
- 최종화면 없이 카드로만 끝내면(직접결제) → 캐시된 미터기값 또는 0원(카드알림이 채움)으로 마감.
- 보강안: 마감 후 120초 내 "입력하신 요금이 맞습니까? Y"가 뜨고 Y≠마감액이면 그 트립 fare를 Y로 갱신(사후 교정).

---

## 3. 플랫폼 콜ID 트립에 저장 (영수증 매칭의 핵심 무기)

- 카카오/티머니 화면·알림에서 **콜ID**를 추출해 트립에 저장(신규 컬럼 `platform_call_id`).
- 영수증 콜ID와 1:1 정확 매칭 → 1번 자동정산의 정확도·시간오차 문제 해결.
- 접근성 화면에 콜ID가 노출되는지 확인 필요(안 뜨면 카드알림/완료화면에서 추출).

---

## 4. 이월 (이전에 합의·미구현)

- **전원 무료 스위치 `free_open`**: 서버 플래그(app_config or env) + `/api/users/:id/flags`에 free_open 반환 + 앱 게이트 `canAutoRecord = free_open || is_admin || auto_entitled` + 대시보드 토글. 유저 1천명 전엔 OFF.
- **다중 플랫폼 "준비(pending) 모드"**: v47/48에 거리게이트로 1차 대응(출발지 재설정 120m 이동 후 확정). 완전판 = "앞 운행 종료 전 잡은 우버/카카오 콜은 pending, 종료+실제 픽업 도착 시 확정." 조합: 우버↔우버, 카카오↔카카오, 우버↔카카오, 티머니→우버/카카오. (카카오/우버 운행 중 티머니는 불가 = 무시)
- **R8 난독화** (`isMinifyEnabled=true`): 디컴파일 방어 + APK 축소. 카카오 SDK/리플렉션 깨짐 테스트 필요.
- **서버 ENFORCE_TOKEN 켜기**: 순번 ID 열람(IDOR) 차단. 구버전 앱 로그아웃 주의 → 무토큰 요청 비율 확인 후.
- **키스토어 안전 보관**: `callradar-release.jks` + 비번 백업(유출 시 사칭·업데이트 불가).

---

## 5. 오늘 v48에 이미 들어간 것 (기록용)

- 탑승 정확도(승객/손님 탑승 화면글자 오탐 제거, 실제 클릭·운행상태만) + 출발지 픽업 재설정(거리게이트)
- 티머니고 완료 인식(카드결제기/도착완료 변주) → 마감(ended_at) → 카드알림 매칭 대상 생성
- 트립 병합 해결(이전 트립 안 닫혀 다음 콜 섞이던 것)
- 근무세션 25h 오염 자가치유 + 퇴근 시 유령세션 누적 차단
- 자동기록 배지(플로팅 버튼 무관, 🟢자동 대기/🔴자동 출발동→현재동)
- 주소 카카오 역지오코딩(동+지번) + 100m 격자 캐시
- 실차시간 boarded_at(탑승~하차), 레이더 오늘/누적/전체 토글
- 테스트기사 실시간 대시보드 `/admin/testers`, 디버그로그 ADMIN_KEY 잠금
- **알림 리스너 재바인딩(requestRebind)** — 재설치 후 카드알림 캡처 끊기던 것 수정 ← **v48 최종, 실검증 필요**

---

## 6. 다음 세션 첫 할 일

1. v48 카드알림 캡처 실검증(앱 열고 다음 카드결제 건 → 자동 채움 확인).
2. 위 1~3(영수증 OCR 자동정산 + #4116 + 콜ID) 구현 → v49.
3. 테스터 3명 대시보드로 우버·대기모드 관찰.
