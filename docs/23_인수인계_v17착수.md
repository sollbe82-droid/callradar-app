# 콜레이더 — 세션 인수인계 (v17 착수) · 다음 방에 넘김

*작성 2026-07-24 (클라우드 세션) · 새 세션은 이거 먼저. 상세: docs/20(이전 인수인계)·21(임포트 설계)·22(v17 작업목록).*

> **중요:** 이 세션은 "클라우드 실행"이라 터미널(git/gradlew)을 직접 못 돌리고 화면 원격클릭으로 우회 실행했음(느림).
> 다음은 데스크톱 앱에서 **"내 컴퓨터에서 실행"** 모드로 시작 → 빌드·배포 빠르게. 처음에 **`C:\CallRadar` 폴더 접근 한 번만** 주면 코드·빌드·서버 전부 커버.

---

## 1. 이 방에서 실제로 끝낸 것 (PC에 저장/반영됨)
1. **v16 서명 AAB 빌드 → Play 심사 업로드 완료.** versionCode 16 / 1.6, `callradar` 키 서명 확인. 산출물 `C:\CallRadar\app\build\outputs\bundle\release\app-release.aab`. 라이트모드 "남은금액" 흰글자→테마색 수정 포함.
2. **서버 배포 완료(Render).** git 정본 = `C:\CallRadar\server`(v18 페어링 포함 상위본)로 확정(인수인계 §4 해소). 공항 hourly 5→12h·`buildHourlyFromCache` 폴백·`expenses/summary` LPG 제외 병합 후 커밋 **6cb1855** → github.com/sollbe82-droid/callradar-server **main push → Render 자동배포**. 라이브 확인(`/health` ok, `/api/devices/0`→`{"devices":[]}` = v18 라이브).
3. **앱 코드 반영(디스크 저장, v17 빌드에 포함될 것):** 최소입력 = 과거기록 저장 시 **목적지 강제 제거**(`RecordsScreen.kt` 337행, 빈 목적지 `미정`) + 기록 날짜 라이트모드 색.
4. **설계 정본 작성:** `docs/21`(유니버설 매출 임포트), `docs/22`(v17 통합 작업목록 10개).

## 2. 진단으로 밝혀진 핵심 (왜 안 되던가)
- **이미지 임포트 안 됨:** 앱 `ImageImportActivity`는 있으나 서버 `/api/import/bulk`(v18)가 **구버전 라이브 서버엔 없어서 404**였을 가능성 → 오늘 배포로 해소. (단 이미지 임포트는 "월별 달력"만 대상 — 하루상세·전표는 미지원)
- **마감 전표 "회사전송만 됨":** 기록추가가 **별도 수동버튼(②-b)** + 그 기록이 **영수증 날짜 아닌 오늘**로 들어감(`DailySettlementScreen`).
- **과거기록 목적지 강제:** `RecordsScreen` 337행 `if(manualDest.isNotBlank())` → 목적지 없으면 저장 무시. (수정 완료)
- **영업일 시작(dayStart):** 설정 UI는 있음(개인·일차·야간). **홈 `/api/today`에만 적용**, 기록·월별·통계·마감 미배선.
- **로그인:** 카카오 **자동로그인 강제** → 기기변경·계정전환 막힘. 레퍼런스 캡처 정답 = **"자동 로그인 = 체크박스(기본 해제)."**
- **전표(3번 영수증) 검산:** 개별거래 합 = 플랫폼 소계 = 총합계 = 211,400 정확 일치(삼중 교차검증 근거).

## 3. 다음 = v17 (docs/22 그대로, 착수 순서)
1. 설정 초기화 감사(전 설정 저장/로드) + 로그인 자동체크박스화·완전로그아웃·계정전환
2. 최소입력(완료 절반)·전표→그날 자동적용(확인형 [예]/[아니요])·플로팅↔전표 빈칸만 채우기
3. 수수료 소수점 · 일고정지출 제거
4. dayStart 전면(서버 /api/trips·stats·summary·expenses + 앱 전화면)
5. 근무세션 퇴근 요약·기록
6. 유니버설 임포트: 파일(엑셀/CSV)·이미지(스샷)
7. 마감 오늘/어제/그제 축소 · LPG 고정금액 실기기 확인
- **v17에서 제외(신규 큰 기능·후속):** 이메일/비번 로그인+회원가입(서버 인증 신설), AI 임포트(손글씨·낯선앱, 유료·서버 신설).

## 4. 배포 방식 (확정)
- **APK 사이드로드 불가(스토어 검증)** → v17도 **서명 AAB → Play 비공개 테스트 업로드 → 80명 테스터 검증**.
- 빌드 = `C:\CallRadar\build_v16.bat`(= `gradlew bundleRelease`). versionCode를 **17**로 올릴 것(`app/build.gradle.kts`).
- 서버 배포 = `C:\CallRadar\server`에서 `git add/commit/push origin main` → Render 자동배포.

## 5. 환경·키
- 서명키 `C:\CallRadar\callradar-release.jks`, alias `callradar`, 비번 = `local.properties`(RELEASE_STORE_PASSWORD/RELEASE_KEY_PASSWORD = callradar2026).
- JAVA_HOME = `C:\Program Files\Android\Android Studio\jbr`. SDK `C:\AndroidSdk`.
- 서버 .env(로컬, git 제외): RENDER_DATABASE_URL, AIRPORT_API_KEY, KAKAO_REST_KEY 등.
- 루이가 직접 할 것(내가 대신 불가): **Play Console AAB 업로드**, 로그인 손보면 **카카오 개발자 콘솔** redirect 확인.

## 6. 다음 세션 시작 프롬프트 (복붙)
```
C:\CallRadar\docs\20, 21, 22, 23 읽고 v17 작업 이어서 해줘.
docs/22 착수 순서대로 구현하고, app/build.gradle.kts versionCode를 17로 올린 뒤
서명 AAB(bundleRelease) 빌드해서 경로 알려줘. 서버 수정 있으면 git push로 Render 배포까지.
- 이메일/비번 로그인, AI 임포트는 v17에서 제외.
- 로그인은 "자동 로그인=체크박스(기본 해제)" + 완전 로그아웃 + 계정전환부터.
- 설정 저장/로드 감사(업데이트해도 값 유지)와 데이터 안 꼬이게 조심.
- 다 되면 Play 비공개 테스트에 올릴 수 있게 AAB 경로랑 내가 할 일 알려줘.
(이 작업은 "내 컴퓨터에서 실행" 모드, C:\CallRadar 폴더 접근 하나로 진행)
```
