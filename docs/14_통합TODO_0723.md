# 콜레이더 — 통합 TODO (2026-07-23 · 오늘 아이디어+버그+미뤄둔 것 합본)
*정본 docs/00·09·10 + 해야할것(0723) + 이번 세션 결정을 중복제거·우선순위화. "한번에" 관점으로 묶음.*

> 대표 정직 노트(헌장): "한번에" 하고 싶은 건 알지만 — **자동화 승인**과 **데이터 뼈대(멱등/기여모델)**는 한 번에 못 넣어요(순서가 생명). 나머지 **서로 독립+심사무해**한 건 이번 라운드에 묶을 수 있음. 아래 🟢가 그 묶음.

---

## ✅ 오늘(이 세션) 이미 끝낸 것
- 특허(가출원) 12발명 ↔ 제품 커버리지 확인(문제 없음, PDF) · **핵심 앵커** docs/_START_HERE 저장
- **과거기록 유니버설 임포트** 설계 정본(docs/12)
- **계정·기기 신원모델**(docs/13) — **서버 API 구현 완료**(페어링·기기플랫폼·계정병합) ※배포(git push)만 남음

---

## 🟢 A. 이번 라운드 "한번에" 묶음 (독립·심사 무해 — 같이 진행 가능)
1. **계정/페어링 앱 UI** — 서브폰 "코드로 연결" · 주폰 "다른 폰 연결(코드표시)" · 기기 플랫폼 다중선택 · "계정 합치기" 버튼 (서버 완료, 앱만)
2. **우버 금액 파싱 95→100** — isUberFareScreen/extractFare 보정 (자동화 승인의 선행 정리도 됨)
3. **과거기록 임포트 1단계** — 파일(CSV/엑셀) import + 확인형 미리보기 (규칙·무료, 심사무해)
4. **이미지 카드 워터마크 공유** — 앱이 직접 카드 이미지 생성(무권한). 지금 텍스트 공유의 상위호환
5. **PDF/이메일 내보내기** — 이미 있는 엑셀의 확장(발명4 세무)
6. **버그 묶음(아래 🔴 중 앱단순수정분)** — 랭킹 길빵 제외, 회화카드 순서, 자정 모니터링 로그
7. **홈 등록소 v2 "튜닝모드" 문서 실물 복원** — 코드 아님, 정본 구멍 메우기(09-G1)

## 🟠 B. 순서 필요 (한번에 X · 토대부터)
- **4-1 전표매칭 경로 결정 → C 권장**: client_uuid(멱등) + 소프트삭제 + fare_inbox 매처(1건자동/다건확인/0건보관). ※멱등은 계정병합 dedup 전제라 우선도↑
- **driver_type 능력 플래그 프로파일 승격** + 네이밍 확정(personal/corporate 정본) → 그 위에
- **⑯ 계약콜 정산**(발명5·docs/05 설계완료) — driver_type 플래그 위에 얹힘
- **데이터 아키텍처 정정**(09): PUT수정·DELETE하드삭제 → 소프트삭제 / receipts에 trip_id 연결

## 🔵 C. 자동화 스토어 승인 (유료화 심장 · 단계별 심사, 별도 트랙)
- **MediaProjection 원탭 금액캡처**(접근성 대체, 발명1·2) — FGS type + 인앱동의 + 시연영상
- 지오펜스+활동인식 자동 시작/종료 경계(기존 위치권한에 얹힘)
- (선택) NotificationListener 보조 — 별도 선언
- 접근성 스크래핑판은 스토어에 올리지 않음(밴 리스크)
- 제출 전략: 한 제출 = 한 민감권한 + 선언 + 영상 (한꺼번에 리젝 회피)

## 🔴 D. 버그·확인 (실기기 검증)
- 금액 합산/파싱: OCR FARE_PATTERNS 2건 합쳐짐·엉뚱금액 (→ 우버 수정과 함께)
- ended_at null (우버/카카오T 종료감지 실패)
- 자정 버그 (서버 정상 → 앱쪽 추적)
- 출발지=목적지 동일 (같은 구 이동, 역지오코딩 한계)
- 카카오T 장거리 분할기록 (lastTripId 리셋 로직)
- 회화카드 순서 이동 · 번역품질(MyMemory→Claude API 경유)
- 직접결제(길빵/예약) 랭킹 집계 제외
- 기술부채: MainActivity 죽은코드·데이터클래스 중복 정리

## 🟣 E. 나중 / 대형 라운드
- 홈/탭 커스터마이징 = 화면 등록소 v2 구현(재사용카드→등록소UI→유저배치저장)
- AI "스며든 AI"(스샷 만능입력→자연어질문→인사이트→핫존) · 하이브리드 비용게이트
- 과거기록 임포트 2·3단계(캘린더 .ics + 스샷 AI)
- 유료 다중 오픈방(2~3개) + 인앱결제/구독
- 구글·네이버 로그인(OAuth 등록)
- 미터기 BLE(제조사 제휴 B2B) · 개인택시 세무패키지 · 영수증 5년 보관

## 📜 F. 사업·법무 (코드 아님 · 병행)
- **테스터 14일 12명 유지**(프로덕션 승격 조건, 이탈 0) — 현 1순위
- 신규 옵트인+설치 활성화(카페·이택교 소개서)
- **특허 정식출원** — ⏰12개월 시계 · 📢플레이 출시=공개 → 공지예외 주장 · 🔒청구항 폭(접근성+인텐트+캡처+지오펜스)
- 상표 출원(9류·42류 "콜레이더")
- 약관·개인정보처리방침 최종검토(PIPA·Play) — 여러기기 통합 동의 추가
- 공항API 5종 승인 · Render 유료 호스팅

---

### 대표 추천 진행안
**지금 라운드 = 🟢A 묶음**(계정/페어링 앱UI부터 → 우버파싱 → 과거임포트1 → 나머지 순차)로 "확 달라진 것"을 테스터에 보이고,
**병행 트랙 = 🔵C 자동화**를 MediaProjection 하나로 시작.
🟠B(뼈대)는 4-1 경로 C 결정 한 번으로 문 열림.
